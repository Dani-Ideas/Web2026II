<header class="topbar">
  <h1 class="topbar__title"><%= title %></h1>
  <div class="topbar__actions">
    <span class="topbar__date"><%= new Date().toLocaleDateString('es-ES', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %></span>
  </div>
</header>
